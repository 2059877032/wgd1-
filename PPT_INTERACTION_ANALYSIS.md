# PPT 图片化展示交互观察

参考网站将 PPT 页面预先导出为图片，并将其排列在一个可拖拽、可滑动的无限画布中。用户先进入独立的 PPT 设计空间，再点击“探索无限画布”，即可在大画布中浏览分散排布的页面缩略图；点击任一页面可放大查看。该方式带来的核心价值是把 PPT 从附件转变为可探索的作品集合。

对于王果典的《秸约未来》，应借鉴“图片化作品集 + 点击放大”的优点，但不直接复制随机分布的无限画布。原因是王果典的 PPT 具有清晰的叙事顺序，采用可翻页的档案式阅读器更便于老师、同学和移动端访客依次理解内容。建议实现为页面图片主阅读器，支持左右翻页、键盘方向键、触屏左右滑动、缩略图页序与点击放大，并保留原始 PPT/PDF 的查看入口。

## 页面资源检查

已从清洁版 PDF 成功导出 20 张 JPEG 页面资源，每页为 1601 × 900 像素，整体约 3.9 MB。首页与第 10 页抽查显示封面、图片、中文正文和图表都保持清晰，适合用于主阅读器；前端实现将采用当前页与相邻页预加载、其余页面懒加载的方式控制首次加载量。

## 前端预览验证

在本地预览中，点击《秸约未来》作品卡后可以打开站内档案放映册。首张封面图成功显示，阅读器会显示 `01 / 20` 页码、上一页与下一页控制、页面索引入口、PDF 查看和 PPTX 下载入口。点击“下一页”后，页面成功切换至第 2 页，页码同步更新为 `02 / 20`，图片文字与版式保持清晰。

页面索引展开后可见 01 至 20 的顺序缩略图，点击第 10 页能够直接跳转至中段技术路线页面，并自动收起索引；主页面与侧栏页码均同步更新为 `10 / 20`。这确认了顺序阅读与自由跳转两种方式均可使用。

大图细看模式也已验证可显示第 10 页的完整画面。首次测试发现浏览器的弹层默认 Esc 处理会先关闭外层阅读器；已将放映册的键盘监听改为捕获阶段处理，并阻止 Esc 继续传播，使其后续优先关闭大图或缩略图索引，再关闭阅读器。

修复后页面已完成热更新并重新载入，下一轮验证将从作品卡重新打开阅读器，再进入大图模式测试 Esc 的层级关闭行为。

重启开发预览后，作品卡仍保持可点击状态，图片化档案放映册的入口、封面页数标记和原始文件入口均正常保留；后续将以显式事件触发方式完成 Esc 的最终复测。

显式派发点击事件后，放映册已成功打开并恢复完整的第一页阅读控制，包括放大查看、上一页/下一页、页面索引以及 PDF/PPTX 文件入口。

最终复测确认：在第 1 页大图细看模式中按 Esc 后，大图层被关闭，而底层档案放映册、首张 PPT 页面、`01 / 20` 页码和翻页控制均保持打开状态。键盘关闭层级已符合预期。

键盘翻页也已验证：按右方向键可由第 1 页进入第 2 页，按左方向键可回到第 1 页，页面编号会同步更新。切换瞬间可能先出现短暂的白色纸页底色，随后由图片资源完成显示，符合纸页转场设计。

等待资源加载后，第 1 页封面图已恢复完整显示，确认短暂纸页底色不会导致页面图片缺失。

为验证慢网或异常资源场景，已新增主页面与缩略图的加载/失败状态，并重新打开放映册准备模拟图片错误事件；这项测试不会修改实际托管资源。

模拟主页面图片错误后，阅读器以“页面图片暂时无法加载”的居中提示替代空白页面，同时保留页码、翻页控制以及 PDF/PPTX 原始文件入口。错误状态反馈已通过实际预览验证。

刷新预览后，模拟错误状态已清除，实际托管资源恢复正常；放映册已重新打开，准备对手机滑动处理逻辑进行事件级验证。

首次事件级测试将触摸开始与结束连续同步派发，未能模拟真实手指滑动之间的时间间隔，因此页面仍停留在第 1 页。下一轮将以异步间隔派发触摸结束事件，验证组件在真实交互节奏下更新触摸起点并完成切页。

以 120 毫秒滑动间隔重新测试后，放映册成功由第 1 页切换至第 2 页，主画面、右侧页码和底部 `PAGE 02 / 20` 进度同步更新。触屏左右滑动逻辑已完成实际验证。

放映册现支持 `?archive=ppt` 直达链接：直接访问该地址会自动打开阅读器，点击关闭按钮后地址会恢复为无查询参数的个人档案首页。这同时支持手机端独立阅读和稳定复测。

缩略图索引已补充首次展开时的显式加载状态初始化。桌面直达阅读器已正常打开，但自动化点击索引时浏览器会话暂时不可用；将改用新建会话与页面级脚本继续验证，不影响实际网站代码或资源。

重新建立直达会话后，页面索引可直接展开并显示全部 20 页缩略图。模拟第 6 页缩略图错误后，索引中该位置明确显示“加载失败”，其余页面与页码仍正常可用，缩略图失败占位已通过实际预览验证。

手机端最终复查覆盖两种关键状态：`?archive=ppt&detail=page` 可直接显示居中的第 1 页细看层与关闭按钮；`?archive=ppt&index=pages` 显示单列阅读器、横向缩略图索引、页码、翻页按钮及“查看 PDF / 下载 PPTX”入口。两种状态均完成实际画面加载，控制区未发生遮挡或超出视口。

为验证弱网时的缩略图反馈，使用 `?archive=ppt&index=pages&preview=loading` 在手机视口展示加载预览：未完成加载的缩略图位置会显示墨绿色旋转标记而非空白，占位区域保持 16:9 尺寸与页码标签。加载中与加载失败两种索引状态均已完成实际画面验证。

PDF 原始文件入口已进一步核验：访问放映册使用的 PDF 地址后，浏览器内置查看器成功载入 20 页文档与左侧缩略图，不需要额外下载即可在线阅读。

PPTX 下载入口也已核验：站内资源路径先返回受控存储重定向，随后得到 HTTP 200 的原始演示文稿响应，下载链路可用。

最后使用 375×812 的真实移动设备指标运行自动化验证：首屏为 `01 / 20` 且主图已加载；模拟从右向左的触屏滑动后页码更新为 `02 / 20`；点击“细看本页”后大图层出现，关闭后恢复阅读器。该移动会话同时识别到 PDF 与 PPTX 两个资源链接；在加载预览状态下，18 个尚未完成加载的缩略图带有实际 `is-loading` 状态。验证截图已保存为 `ppt-mobile-validation.png`。

在同一真实移动设备会话中，已实际触发 PDF 与 PPTX 两个入口：PDF 在当前会话中跳转至签名存储地址并成功打开；PPTX 下载在指定目录落地为 `straw-future-clean_95f53e3e.pptx`。至此，触屏翻页、细看打开与关闭、主图加载、缩略图加载/失败状态、PDF 在线阅读和 PPTX 下载均完成移动端逐项验证。

桌面与手机全页截图均确认作品集入口仍与既有编辑档案风格一致：桌面端的封面和项目说明保持并列层级，手机端自动改为自上而下的单列阅读顺序。PPT 入口、页数标记与下载入口在两种视口下均清晰可见。
