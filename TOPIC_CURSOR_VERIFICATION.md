# 专题阅读室与叶脉检索标验证记录

## 专题阅读室布局

桌面端首页在身份书本之前新增三张专题入口卡：**药研笔记、AI 工坊、表达与策展**。卡片按照“学习路径—AI 项目—PPT 放映册”的逻辑分别连接到现有档案位置，形成从身份概览到具体成果的可读路径。

手机端三张卡片自然收为纵向单列，文字、入口箭头与装饰图形均保留在可见范围内，未发生重叠或按钮截断。移动端不启用自定义鼠标光标，因此保持系统原生触控体验。

桌面端实际预览已识别三张专题入口及它们分别指向的 `#path`、`#ai-work` 与 `#ppt-archive` 锚点。下一步将继续验证鼠标进入不同交互目标后“叶脉检索标”的状态变化。

实际点击“药研笔记”入口后，浏览器开始平滑滚动至学习路径阅读区；专题入口保持常规锚点链接，因此同时支持鼠标点击、键盘 Enter 和移动端触控。

叶脉检索标已完成桌面端状态验证。鼠标进入专题入口后，根元素状态为 `page`，提示标签在过渡结束后完全显示；进入坦克大战的独立试玩入口后，状态切换为 `launch`，提示标签同样显示。三张专题入口均为原生链接，实际 `tabIndex` 为 `0`，可由键盘顺序聚焦。

目录按钮与身份书本翻页控件已分别获得 `section` 与 `turn` 的独立光标模式，区别于专题入口的 `page`、PPT 查验的 `verify` 与游戏试玩的 `launch`。将焦点置于“药研笔记”后实际按下 Enter，浏览器地址更新为 `#path` 并执行站内跳转，说明键盘可直接激活专题阅读路径。

针对章节目录，已补充打开后将焦点自动移入目录关闭按钮、并在弹层内循环 Tab 焦点的逻辑。复测前，目录触发按钮可被程序化焦点定位，并显示浏览器默认的可见焦点轮廓；下一步将以实际 Enter 与 Tab 验证弹层内焦点位置。

复测结果：实际按 Enter 打开目录后，焦点自动落在目录内部的“关闭目录”按钮，且该控件保持可见焦点轮廓。随后实际按 Tab，焦点进入目录中的第一条章节入口（A/01），仍处在目录弹层范围内，验证了键盘顺序导航不会跳离当前可见的操作区域。

通过 DOM 读取进一步确认：Tab 后 `document.activeElement` 为目录内首个 A/01 章节按钮，具有 `outline: auto`、`outline-width: 1px` 的可见焦点样式。随后实际按 Enter 激活该章节项，目录关闭并触发首页个人索引的纸页转场与站内跳转，完成了“专题卡—目录按钮—目录章节项”的 Tab/Enter 键盘导航链路验证。


## 高辨识度叶片指针修订

原先的环形轮廓已替换为更明确的叶片指针：深墨绿色双线叶缘、叶片中脉、两侧短叶脉、叶柄和浅绿色透明填充共同构成叶形；基础尺寸扩大到约 42×30 像素，悬停入口时扩大至 46–62 像素，并配合纸张色外描边和阴影提升在暖白背景上的可见性。普通浏览时实际预览中能够直接辨认出叶尖、叶片和叶柄，悬停链接时仍显示“前往”标签。移动端 375×812 视口截图确认未显示自定义光标，页面保留原生触控和原生布局。

## 精修版复测记录

本轮将叶片轮廓收窄为约 1.35px 的细墨线，缩小中心光点与叶柄，并将填充调整为半透明苔绿色；状态尺寸同步收敛，避免叶片在悬停时过度遮挡文字。桌面端实际预览可辨认叶尖、叶缘、中脉、短叶脉与叶柄，鼠标移动时叶片随指针稳定跟随。

点击目录按钮后，精修版会触发一次短暂的低透明度圆形墨水落点，约 420ms 内自然淡出；点击反馈不再使用明显的外扩光环，减少粗糙和游戏化感。目录弹层打开后，实际按 Tab，焦点仍进入首个 A/01 章节按钮并保持可见焦点轮廓；移动端 375×812 视口复测确认不显示自定义光标，继续使用原生触控。`prefers-reduced-motion: reduce` 下点击墨水动画被关闭。

本轮浏览器复测补充证据：重新加载桌面预览后，首页截图中叶片指针位于主视觉文字与图片之间，形状、叶尖和中脉可见；点击目录按钮后，目录弹层打开，点击位置附近保留叶片指针状态。随后按 Tab，弹层内第一条 A/01 章节按钮出现可见焦点轮廓，说明键盘焦点仍进入预期的目录操作链路。浏览器截图工具无法直接读取 CSS 动画的逐帧像素值，因此墨水落点的确认以代码规则、触发路径和点击后即时状态截图共同记录，不虚构额外的逐帧测量结果。

控制台复测结果：目录打开后读取到 `document.activeElement` 为 `BUTTON`，文本为 `A/01个人索引中药学、AI 与实践的交汇`，焦点样式为 `outline: auto 1px`。模拟真实 `pointermove` 到 `(318, 246)` 后，叶片容器变换矩阵为 `matrix(1, 0, 0, 1, 318, 246)`；随后触发真实 `pointerdown`，立即读取到 `data-leaf-cursor-pulse="true"`，叶片边框为 `1px rgb(23, 79, 66)`，尺寸为 `37px × 26px`。这组结果直接确认了键盘焦点、鼠标跟随矩阵和点击反馈触发链路；逐帧淡出过程仍以 CSS 动画规则记录。

## 多形态配色与信封状态复测

本轮将叶片主体改为深墨绿色 `rgb(18, 63, 53)`，中脉、短叶脉和叶柄改为浅苔绿色 `rgb(182, 216, 180)`。联系入口统一使用 `data-cursor="envelope"`，包括页首联系按钮、首页身份索引入口、电话、微信和邮箱。

浏览器控制台将页面置于 `envelope` 状态并等待过渡完成后读取到：信封尺寸 `44px × 29px`、圆角 `2px`、封套背景 `rgb(18, 63, 53)`，前后折线均为 `rgb(182, 216, 180)`。其他状态仍保留章节、阅读、翻页、查验和试玩的不同尺寸与旋转方式。桌面端首屏截图确认新配色与档案纸张背景协调；375×812 移动端截图确认不显示自定义光标并继续使用原生触控。

补充复测中，控制台逐一切换 `browse`、`section`、`read`、`turn`、`verify`、`launch` 与 `envelope`，读取到的尺寸和变换矩阵均不同；信封状态最终尺寸为 `44px × 29px`、圆角 `2px`，背景图为纸张色上层与深绿封套下层的渐变，折线为浅苔绿色。实际点击页首“联系”入口后地址跳转到 `#contact`，联系区域正常出现，说明信封状态已绑定到真实入口。随后读取焦点为 `BUTTON`，轮廓为 `auto 1px`，点击事件仍触发 `data-leaf-cursor-pulse="true"`，移动端继续使用原生触控。

## 自然形态重构复测

参考自然植物线稿、动漫植物插画中的弧形中脉与分叉叶脉，以及折纸信封的翻盖三角和内页折线后，本轮不再使用贯穿叶片的直线。叶片中脉改为带 `54% 46%` 弧度的边框曲线，分支叶脉采用 `70% 30%` 的不对称弧形，并保留深绿叶片与浅绿线条的层次。控制台读取到叶片分支的 `border-radius: 70% 30% 0 0`，确认曲线结构已写入实际计算样式。

信封光标改为纸张色渐变内页、深绿封套、浅绿折线和三角翻盖；控制台读取到尺寸 `44px × 29px`、圆角 `2px`、背景渐变 `#f7f5ed → #123f35`、翻盖 `clip-path: polygon(0 0, 100% 0, 50% 100%)`，不再是简单的两条交叉直线。桌面端和 375×812 移动端截图均通过，9 项测试与生产构建通过。

自然形态版本的独立控制台复测结果：将目录按钮聚焦后，`document.activeElement` 为 `BUTTON`，文本为“目录 CONTENTS”，焦点样式为 `outline: auto 1px`；模拟鼠标移动到 `(376, 214)` 后，叶片容器变换矩阵为 `matrix(1, 0, 0, 1, 376, 214)`；随后触发点击事件，立即读取到 `data-leaf-cursor-pulse="true"`。同时读取到中脉弧度 `54% 46% 0 0`、分支弧度 `70% 30% 0 0`，确认本轮重构后的自然叶脉、鼠标跟随、点击反馈和键盘焦点均已在同一版本上复测。

## 叶脉内部锚点修复

针对实测截图中浅绿色叶脉从叶片旁边悬浮的问题，本轮将叶片容器设置为 `overflow: hidden`，并把主脉缩到叶片内部：叶片尺寸为 `37px × 26px`，主脉起点为 `6.3px × 10.3px`、宽度 `21px`，分支叶脉起点为 `17px × 10px`，弧度为 `70% 30% 0 0`。控制台确认叶片状态 `overflow: hidden`，因此浅绿色叶脉无法越过深绿色叶片边界；切换到信封状态后叶脉 `display: none`，信封翻盖仍使用独立三角结构。

叶柄连续性复测：将 `.leaf-cursor-vein` 作为叶片基部到主脉的连续叶柄，控制台读取到位置 `left: 4px; top: 17px`、宽度 `12px`、弧形 `border-radius: 70% 30% 0 0`，并与叶片容器的 `overflow: hidden` 一起保证叶柄留在深绿色叶片内部。

四状态独立复测：`browse` 为 `37px × 26px`、叶柄显示；`read` 为 `41px × 29px`、叶柄显示；`turn` 为 `47px × 30px`、叶柄显示；`envelope` 为 `44px × 29px`、翻盖 `clip-path: polygon(0 0, 100% 0, 50% 100%)` 且叶脉 `display: none`。四种状态均读取到 `overflow: hidden`，确认叶脉不再越出主体，信封也不复用叶脉结构。

叶柄—主脉相对坐标证据：在 `browse` 状态下，主脉伪元素相对叶片起点为 `left: 6.3px; top: 10.3px; width: 21px`，叶柄起点为 `left: 4px; top: 17px; width: 12px`。二者在叶片内部的横向范围分别为约 `6.3–27.3px` 与 `4–16px`，纵向范围在 `10.3–19.1px` 与 `17–24px` 发生重叠，且叶柄向主脉方向旋转 `-38deg`；这证明叶柄末端进入主脉起始区域，而不是悬浮在叶片外部。叶片容器继续使用 `overflow: hidden` 作为边界保护。

## 阅读状态截图复现修复

针对最新截图中“阅读 / 前往”提示覆盖叶片的问题，阅读实践档案入口现已明确绑定 `data-cursor="read"`，不再落入通用 `link` 状态。控制台复测读取到 `cursorMode: read`，叶片实际范围为 `left: 137.5px–181.3px`、`top: 367.8px–412.2px`，提示标签范围为 `left: 204px–245.6px`；标签已经位于叶片右侧，不再覆盖叶片主体。提示标签 CSS `left: 48px`、`z-index: 2`，叶片仍通过 `overflow: hidden` 保持内部叶脉完整。

## 统一 SVG 叶片重构

针对截图中叶脉仍像独立线条的问题，本轮将叶片主体、主脉、四条分支叶脉和叶柄改为同一枚内嵌 SVG 背景路径，彻底停止依赖会发生相对偏移的独立伪元素。SVG 使用深墨绿色叶片路径、浅苔绿色主脉与弧形分支，叶柄从叶片基部进入主脉，所有线条随同一个叶片坐标系缩放和旋转。

最新控制台逐一读取 `read`、`browse`、`turn`、`envelope`：前三种状态均读取到同一 SVG `background-image`，尺寸分别为 `41px × 29px`、`37px × 26px`、`47px × 30px`；信封状态读取到独立纸张渐变，尺寸 `44px × 29px`。阅读入口的 `data-cursor` 为 `read`，提示标签为 `left: 48px; top: -12px`，不再覆盖叶片。统一 SVG 版本通过 9 项测试和生产构建。

## 点击微动、柔光与尺寸平衡

本轮为叶片与信封共用 `cursor-leaf-tap` 点击动画：动画时长 `0.36s`，使用独立 `scale` 与 `translate` 属性完成约 `0.91` 倍收缩、轻微下沉、`1.035` 倍回弹和短暂上浮，不改变叶片原有旋转方向。普通状态和信封状态均读取到白色柔光 `drop-shadow`，基础柔光为 `rgba(255,255,255,.42)`，点击时提升至 `.52`，便于在深色背景中辨认。

控制台读取到 `browse`、`read`、`turn` 和 `envelope` 四种状态的最终计算尺寸均为 `46px × 37px`；信封不再明显小于叶片。移动端仍由媒体条件保留原生触控，不显示桌面专属光标。

375×812 移动端独立复测：首屏标题、阅读入口、联系方式与目录均保持稳定布局，未显示桌面专属叶片或信封光标，触控入口仍以原生按钮和链接交互；页面未出现横向溢出。点击微动与柔光仅在桌面指针媒体条件下启用，不干扰移动端触控。

点击微动版本最终状态复测：`browse`、`read`、`turn`、`envelope` 均读取到 `cursor-leaf-tap` 在脉冲状态下 `0.36s`，普通状态读取到白色 `drop-shadow`，四种状态尺寸统一为 `46px × 37px`；信封状态背景为纸张渐变，叶片状态背景为统一 SVG。

## 移动拖尾验证

桌面端连续触发 12 次真实 `pointermove` 后，控制台读取到 12 个短命粒子，其中包含 `leaf-trail-particle leaf` 与 `leaf-trail-particle star` 两种类型；粒子动画为 `leaf-trail-drift`、时长 `0.68s`，`pointer-events: none`，约 `0.8s` 后自动清理为 0 个，证明拖尾不会持续堆积或阻挡页面交互。拖尾生成由距离与时间双重节流，且在 `prefers-reduced-motion: reduce` 下通过 CSS 隐藏。

375×812 移动端独立截图复测：首屏、联系方式、目录和触控入口布局稳定，没有显示桌面拖尾粒子或桌面叶片光标，移动端继续保留原生触控。

拖尾版本的最终光标复测：`browse` 读取到约 `46.05px × 37.03px`，`read` 约 `48.94px × 38.95px`，`turn` 约 `50.89px × 39.94px`，`envelope` 约 `49.03px × 38.03px`；四种状态均读取到白色柔光 `drop-shadow`。真实脉冲状态再次读取到 `cursor-leaf-tap`、`0.36s` 动画和点击增强柔光 `rgba(255,255,255,.52)`，确认拖尾新增没有覆盖既有点击微动或多状态光标。

拖尾频率调整复测：触发 20 次、间隔 34ms 的连续指针事件后，读取到 20 个活动粒子，其中 15 枚叶片碎片、5 枚星光粒子；粒子仍使用 `leaf-trail-drift`、`0.68s` 动画和 `pointer-events: none`，约 0.85 秒后清理为 0 个。代码阈值由 `12px / 42ms` 调整为 `8px / 30ms`，属于适度增密而非无限生成。

频率调整版本的补充复测：浏览器控制台再次读取 `cursor-leaf-tap`，动画时长为 `0.36s`，点击增强柔光仍为 `rgba(255,255,255,.52)`；375×812 移动端截图显示首屏、联系方式和目录布局稳定，未显示桌面叶片光标或拖尾，原生触控入口保持可用。

## 首页动态感验证

首页首屏新增档案式多层视差：文案层使用低幅度 `translate`，作品图像、纸张底层、椭圆档案线、FIELD / 00 印记和右下角注记使用不同位移幅度与阻尼。控制台在鼠标坐标约 `0.71875 / -0.6727` 时读取到文案层 `translate: 0px 0.2236px`、图像层约 `1.565px` 垂直位移、椭圆线约 `-1.565px` 位移和印记约 `-1.118px` 位移，证明各层确实产生错层响应；文案层改用独立 `translate`，避免被原有 `rise` 入场动画覆盖。

桌面端截图显示动态装饰仍保持暖白纸张、墨绿档案和细线层次，没有大幅晃动；375×812 移动端截图显示视差装饰隐藏、首屏排版稳定，继续使用原生触控。生产构建和 9 项测试通过。

首页动态版本补充证据：滚动到 `reading-rooms` 后，控制台读取到该章节已加入 `is-visible`，证明 IntersectionObserver 的轻量滚动进入过渡实际触发；控制台直接聚焦目录按钮和“阅读实践档案”入口时，`document.activeElement` 分别为 `BUTTON.header-directory` 与 `A.ink-button`，两者均为 `tabIndex: 0`，且读取到浏览器默认可见 outline。

减少动态效果复测：代码包含针对 `scroll-reveal`、首页视差层和拖尾的 `prefers-reduced-motion` 静态降级声明；在控制台模拟降级样式后，章节样例读取为 `opacity: 1`、`transform: none`、`transition: none`，页面保留完整内容而不依赖动画显现。移动端截图与入口数量复测均显示布局和可操作元素保持稳定。
