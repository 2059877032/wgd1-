# 首页动态感参考记录

## 参考来源

用户上传视频：`/home/ubuntu/upload/pasted_file_xs67FR_20260816-2329-14.7768346.mp4`。该记录用于提炼交互规律，不复制原页面素材。

## 视频中可借鉴的动态规律

视频的核心是持续触发式多层视差。背景装饰层只做很小的反向移动；描边或装饰文字层移动幅度更大，并带有延迟；核心实心标题保持更稳定；前景卡片有轻微的 3D 倾斜与悬浮感。整体响应不是循环动画，而是跟随鼠标坐标连续变化，并使用平滑阻尼。

## 转化为王果典个人档案网站的边界

保留“多层空间感”和“鼠标可控反馈”，但将科技几何转化为纸张、描图纸、档案索引线和墨绿色装饰。位移应克制，避免大幅晃动；文字保持可读和稳定；移动端不启用桌面视差，继续使用原生触控；用户设置 `prefers-reduced-motion` 时关闭非必要动效。

## 本轮实现目标

首页首屏使用低幅度文案层、图像层、注记层和档案线条层视差，并让作品入口卡片保持轻微的纸张悬浮反馈。所有动态只使用 `transform` 与 `opacity`，不改变布局流；鼠标拖尾继续由现有系统负责，不与首页视差层重复生成。
