# 移动端目录返回阅读位置：验证记录

## 2026-08-17 首轮功能检查

已确认目录可打开，并且返回动作可关闭目录。桌面浏览器自动化环境中，`window.scrollTo({ top: 2600 })` 的实际滚动读数没有稳定到预期值（记录值为 10，恢复后为 908），因此该次结果不能作为位置精确恢复的通过依据。下一步将以页面原生滚动、移动视口截图和可重复的 DOM 读数继续验证；实现本身使用记录的 `window.scrollY` 和 `behavior: "auto"`，不会使用平滑滚动。

随后通过真实页面滚动进入中段阅读位置；浏览器报告页面上方已有 3562px 内容、下方剩余 6915px，可用于下一轮目录打开与返回检查。

## 通过：真实阅读位置恢复

在该中段位置点击目录后，返回动作的浏览器结果为：`before: 3562`、`after: 3562`、`directoryClosed: true`、`focusReturnedToDirectoryTrigger: true`。这确认返回按钮会关闭目录、使用即时定位保留原阅读位置，并把键盘焦点以 `preventScroll` 方式交回固定在顶部的目录入口。
