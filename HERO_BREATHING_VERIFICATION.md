# 首页逐字呼吸动效验证记录

## 2026-08-17

首页核心文字已拆为独立字元并采用 `hero-glyph-breathe` 动画。控制台读取结果如下：

| 文字组 | 独立字元数 | 首末字延迟 |
| --- | ---: | --- |
| 王果典 | 3 | `0s` / `-0.24s` |
| 核心定位 | 28 | `0s` / `-2.66s` |
| 首屏说明 | 55 | `0s` / `-3.888s` |

按钮与联系方式没有逐字动画；图片仅保留原有首屏入场动画。将 `data-reduced-motion` 设为 `true` 后，抽样字元的动画名为 `none` 且 transform 为 `none`。
